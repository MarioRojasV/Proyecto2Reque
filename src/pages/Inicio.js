import React from 'react';

const Inicio = () => {
    return (
        <div style={styles.container}>
        </div>
    )
};

const styles = {
    container: {
        padding: '2rem',
        textAlign: 'center',
    }
};

export default Inicio;